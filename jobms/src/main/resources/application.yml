spring:
  application:
    name: jobms

  datasource:
    url: jdbc:postgresql://localhost:5432/jobapp_db
    username: embarkx
    password: embarkx

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8082



